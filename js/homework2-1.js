// build a function that takes in an array of strngs and returns an array for each array item
//it returns one array that has two items
//one is a boolean - are there anagrams in this array
//and the other is how many
​
//['raey', 'year'] -> [true, 1] // cause there are only one pair
//return new array with 1st value being true and 2nd calue being 2/however many anagrams
​
// Input (An array)
// Output (an array with two values: 1)a boolean iif theres an anagram present 2) count of anagram pairs

// Psuedocode:
// Iterate through the array of strings
// node homework2-1.js
